# Preprocess

```{r}
library(Seurat)
seurat_obj <- readRDS(file = params$rds)
seurat_obj

seurat_obj@assays$RNA@counts[1:5, 1:5]
VlnPlot(seurat_obj, features = c("nFeature_RNA", "nCount_RNA"), ncol = 2)
FeatureScatter(seurat_obj, feature1 = "nCount_RNA", feature2 = "nFeature_RNA")
```
